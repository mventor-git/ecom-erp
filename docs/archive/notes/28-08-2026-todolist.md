# Implementation Tracker — Ecom-ERP ERP + E-Commerce — 28-08-2026

Status legend:
[ ] NOT STARTED  [~] IN PROGRESS  [x] COMPLETED  [!] BLOCKED  [?] NEEDS DECISION

Driving documents:
- `gpt-web-vision.md` — long-term vision (ONE BUSINESS → ONE SOURCE OF TRUTH → ONE CONNECTED OPERATIONAL STORY; traceable stories; ledger-based inventory; historical costing; role-aware experiences; control-center dashboard; AI after trusted data).
- `gpt-web-master-prompt.md` — master handoff / recovery & forward build brief. Mandates: current-state audit FIRST (no code changes before it), evidence-only verification (VERIFIED / PARTIAL / BLOCKED / BROKEN / NOT VERIFIED), no DB resets, no duplicate systems, one authoritative stock mutation per event, idempotent retries, backend-authoritative auth, Packing E2E (14.7) as first delivery target.
- `SKILL.md` — ERP/warehouse/accounting domain skill (non-negotiable distinctions, ledger model, FIFO, journal-entry principles, testing matrix).

Master-prompt operating rule:
DISCOVER → UNDERSTAND → FIX → CONNECT → TEST → PROVE → ONLY THEN → MOVE FORWARD.
Never claim VERIFIED without multi-layer evidence (UI → API → business logic → DB → auth → runtime → notification → audit → idempotency → regression).

---

## Phase 0 — Current State Audit (DONE 2026-08-28)

Master prompt §42–§47, §56. Output: `docs/system-audit-2026-08-28.md`.

- [x] 0.1 Full-repo audit executed (29-agent workflow: 8 map + 16 domain audits + 4 adversarial verifications + 1 synthesis)
- [x] 0.2 Audit report sections A–N delivered in `docs/system-audit-2026-08-28.md`
- [x] 0.3 Domain audit table produced (16 domains, honest statuses)
- [x] 0.4 Route audit table produced (31 sidebar targets; canonical admin prefix = root + /erp; dead links flagged)
- [x] 0.5 Runtime audit produced (5172/5173/5174 live PIDs + behavior)
- [x] 0.6 DB audit produced (table counts; no modifications)
- [x] 0.7 Business flow audit produced (Customer → Order → Issue Receipt → Inventory → Picking → Packing → Delivery/Returns future)
- [x] 0.8 TODO reconciled with audit findings (no false VERIFIED)

## Phase 1.0 — Customer Checkout Fix (COMPLETED + VERIFIED 2026-08-28)

Highest-priority blocker from the audit: **Customer checkout was BROKEN** (CartPage payload did not match the backend session contract; no order was ever created; client read `checkout_url` but backend returns `pay_url`).

- [x] Root cause identified (contract mismatch: client sent `{items,customer_email,phone,...}` to `POST /api/kashier/checkout/session` which requires `{orderRef,total,customerEmail,redirectPath}`; no order creation step; `checkout_url` vs `pay_url`)
- [x] Authoritative contract preserved from backend (`POST /api/orders` + `POST /api/kashier/checkout/session`); client aligned to it (NO frontend-only workaround)
- [x] CartPage now: create order (idempotent) → Kashier session for that order → redirect to `pay_url`
- [x] Idempotent order creation: `idempotency_key` column added; retry with same key returns the SAME order (no duplicates)
- [x] Kashier session endpoint hardened: order-exists validation, `allowedMethods` honored (card vs wallet), `order_id` linked to `kashier_orders` (webhook fallback)
- [x] Pre-existing bug fixed: `orders.stripe_session_id` UNIQUE collided on second non-Stripe order → now stores `null` for no-session orders (SQLite UNIQUE allows multiple NULLs)
- [x] Pre-existing bug fixed: `kashierWebhookService.handleTransactionSuccess` did `SELECT status FROM orders` (never selected `items`) and called `parseItems(order.items)` (passed the string, not the row) → webhook-paid orders NEVER deducted stock nor created the pick/pack Issue Order → now `SELECT id,status,items` + `parseItems(order)`
- [x] Static: client build ✓ (12.7s) + client-admin build ✓ (11.4s), exit 0
- [x] Runtime E2E (live 5172, `server/_checkout_e2e_full.js`): product create → order (idempotent) → session contract → signed webhook → order PAID + single authoritative `issue` movement (−1, 50→49) + auto Issue Order `ISS` created
- [x] Webhook idempotency: duplicate event → `duplicate:true`; bad signature → 401; order state `paid` exactly once
- [x] Regression probes: `/api/orders/statuses` 200, `/api/kashier/checkout/status` 200, `/api/products` 200, `/api/admin/customers` unauth 401, `/api/admin/inventory/stock` unauth 401
- [x] Customer/Admin source separation + Issue Receipt single-authority design preserved (no duplicate systems, no DB reset)

Evidence (files changed):
- `client/src/pages/CartPage.jsx` — coherent checkout flow (createOrder → createKashierSession → `pay_url`); deterministic idempotency key
- `server/routes/orders.js` — idempotency_key support; kashier methods skip immediate stock issue (webhook is authoritative); `stripe_session_id || null`
- `server/routes/kashierCheckout.js` — order-exists validation, allowedMethods passthrough, `order_id` linkage
- `server/services/kashierWebhookService.js` — `SELECT id,status,items` + `parseItems(order)` (fixes silent no-deduct on payment)
- `server/db.js` — `orders.idempotency_key` column (ALTER, safe pattern)
- Evidence harness: `server/_checkout_e2e_full.js` (kept as reproducible runtime proof)

## Phase 1.1 — DB Migrations 004–007 (COMPLETED + VERIFIED 2026-08-28)

Audit blocker L4: migrations were never applied (order_items legacy 9-col, `cost_consumption` missing, VIP tables missing).

- [x] Applied `004` (order_items 19-col design is a no-op on existing table; `orders.sales_channel` added)
- [x] Applied `005` (order_items ALTERs: variant_id/sku/base_price/discount_*/final_price/price_list_code/tax_*/cost_snapshot; created `vip_customer_policies`, `customer_invitation_links`)
- [x] Applied `006` (inventory_cost_layers + indexes — already present, indexes ensured)
- [x] Applied `007` (created `cost_consumption` + indexes — FIFO COGS persistence foundation)
- [x] Added explicit `order_items.qty` column (004's CREATE TABLE is a no-op on the existing legacy table, so `qty` was never added) + backfilled from legacy `quantity`
- [x] Backfilled 3 legacy order_items rows: `base_price=final_price=price(1500)`, `qty=12` — no data loss (count preserved: 3)
- [x] Applied with backend stopped (no live-DB overwrite), additive-only SQL, backup `store.db.migrate-004-007.bak`, no reset
- [x] Restart + persistence verified: schema survives restart, all apps healthy (5172/5173/5174)
- [x] Regression: checkout smoke test — order create 201, idempotent retry same order, session contract accepted; `/api/products` 200, `/api/orders/statuses` 200
- [x] Evidence harness: `server/_migrate_004_007.js` (idempotent, re-runnable)

## Phase 1.2A — RBAC Permissions Gap (L2) + VIP unblock (COMPLETED + VERIFIED 2026-08-28)

Audit blocker L2: `customers.manage`/`customers.view` enforced in `routes/vipInvitations.js` but missing from the 33-permission DB catalog → VIP always 403; `/api/admin/vip` route was dead code (after `module.exports` in index.js).

- [x] Added `customers.view` (id=34) + `customers.manage` (id=35) to permissions; assigned to admin (35 total) + sales_employee (4 total)
- [x] Verified: `usersWithPermission('customers.manage')` → [1,2]; `customers.view` → [1,2]
- [x] Fixed dead code: mounted `/api/admin/vip` + `/api/admin/recommendations` BEFORE `module.exports` in `server/index.js`
- [x] Fixed VIP POST schema collision: `vipInvitations.js` wrote `invite_code`/`segment_type`/`discount_pct` but `vip_invites` table has `code`/`invite_name`/`created_by` — aligned to actual schema (matches `vipService`)
- [x] Fixed recommendations route `quantity_change` → `qty_change` (inventory_movements column) — surfaced by mounting
- [x] Tested (HTTP): admin login 200; GET /api/admin/vip 200; POST /api/admin/vip 201 (invite created); GET after create 200 (2 invites); GET /api/admin/recommendations 200; anon 401; customers 200
- [x] Evidence: `server/_fix_rbac_perms.js` (idempotent permission seed)

## Phase 1.3 — Clean Seed + Users + Architect Review (COMPLETED 2026-08-29)
- [x] Created `delivery_employee` role + `warehouse`(warehouse123)/`delivery`(delivery123) users
- [x] Seeded 5 categories (no emoji), 4 brands, 10 Ecom-ERP products with stock (10 opening-balance movements)
- [x] Fixed `GET /api/admin/users` 500 (`u.updated_at`/`r.description` columns don't exist — removed from query)
- [x] Verified: all3 logins (admin/warehouse/delivery), products10, inventory10, users3, frontends build+serve

## Architect Review — Findings Queue (loop until done)

Patterns being hunted: duplicate pages/functions, unusable UI elements, hardcoded values, emoji violations, placeholder-as-implemented, missing i18n.

- [x] EMOJI violations fixed (zero-emoji rule): DashboardDetailPage, ProductsTrash, CategoriesList, ProductsList, IntegrationsPage, PricingEngine, ShippingDashboard, Picking/Packing (all → Lucide icons)
- [x] VipInvitations.jsx rewired from placeholder (local-state only) to real API (getVipInvites/createVipInvite) + real vip_invites schema (code/invite_name/used status)
- [x] Verify PickingDashboard/PackingDashboard clean of emoji (verified — Lucide icons)
- [x] Scan for duplicate functions/pages: deleted proven-dead files (MovementSubPage, RecommendationsPage, InventoryMovementPage); PickingDashboard noted as minor duplication of PackingDashboard's picking tab
- [ ] Customer site i18n gaps (LARGE, deferred): ProductDetailPage ~180, AccountPage ~92, OrderTrackerPage ~81, SuccessPage ~15 hardcoded strings
- [ ] Legacy admin files in customer bundle (AdminDashboard, AdminSalePage, ProductForm) — proven unused, defer removal
- [x] Fix placeholder-as-implemented: VipInvitations (rewired to API); /inventory/movement → real MovementsList (mockup removed); created ProductForm for /products/new + /products/:id/edit (replaces mockup)
- [x] Fix dead links: /erp/returns removed (future work, no placeholder page); /notifications → /erp/notifications; catch-all 404 added

## Phase 1 — Fulfillment Core (priority 2 per master prompt §57)

Goal: every major business process has ONE coherent source of truth and ONE connected workflow.

### 1.1 Packing E2E (master prompt 14.7 — COMPLETED + VERIFIED 2026-08-28)
- [x] Audited current Packing (service/routes/UI/table/RBAC) — found gaps, no blind implementation
- [x] Defined Issue Receipt → Picking → Packing handoff (auto-create on picking completion verified)
- [x] Packing data model: reused existing `packing_tasks` schema (no invented tables)
- [x] Ordered transition guard added: pending→in_progress→packed→completed, problem flow, invalid transitions rejected
- [x] Packing task creation from real Picked task (order transition to 'packing' → createPackingTask)
- [x] Assignment → start → pack (real task, RBAC backend-enforced via `inventory.packing`/`inventory.view`)
- [x] RBAC audited: `inventory.packing` on status, `inventory.manage` on assign, `inventory.view` on list/stats — all present in catalog
- [x] NOTIFICATIONS proven: 22 packing in-app notifications (assign + task created + status)
- [x] AUDIT proven: 5 `packing_status_changed` events per lifecycle
- [x] PRINT proven: packing-sheet endpoint (`/api/admin/view/packing/:orderId`) + `printService.packingSheetHtml` (HTML, QR/barcode) — new
- [x] Inventory rule: ZERO inventory movements from packing (VERIFIED — Issue Receipt stays the authority)
- [x] Idempotency: `packed_by`/`packed_at` preserved on retry (packed→packed); re-pack after problem is a real transition
- [x] Concurrency: backend state protection via ordered transition guard (single authoritative state)
- [x] Tests: E2E harness `server/_packing_e2e_service.js` (all DoD assertions pass); admin build ✓
- [x] UI: PackingDashboard emoji → Lucide icons (zero-emoji rule), packing-sheet button, action buttons aligned to transition map

Evidence (files changed): `server/services/pickingPackingService.js` (transition guard + idempotency), `server/services/printService.js` (packingSheetHtml), `server/routes/views.js` (packing sheet route), `client-admin/src/api/adminApi.js` (viewPackingSheetUrl), `client-admin/src/admin/pages/PackingDashboard.jsx` (icons + sheet button).

Note: pre-existing `tests/pickingPacking.test.js` is incompatible with current `listPickingTasks` return shape (`{items,pagination}` vs array) — unrelated to this change; E2E harness used instead.

### 1.2 Delivery E2E (14.8)
- [ ] Delivery states, driver assignment, proof-of-delivery, delivery confirmation updates (NOTIFICATION + AUDIT + no double inventory)
- [ ] Mark as active workflow only when real; otherwise clearly unavailable (no placeholder-as-implemented)

### 1.3 Returns / Refund foundation (14.9)
- [ ] Return eligibility (days, order status, delivery completed, previous return status)
- [ ] Return receipt / return order; driver pickup; warehouse inspection (RESELLABLE / DAMAGED / MISSING / USED / OTHER)
- [ ] Restock (inventory movement) or damaged-stock movement; COGS/revenue reversal
- [ ] Refund linked to return + inspection + accounting; duplicate-refund protection

### 1.4 Unified Order / Fulfillment Timeline (15)
- [ ] ONE Order tells the whole story: Created → Payment → Issue Receipt → Picking → Packed → Driver Assigned → Out for Delivery → Delivered
- [ ] Commercial / financial / operational / inventory states kept distinct (not collapsed into one vague status)

## Phase 2 — Procurement + Inventory (priority 3)

- [ ] 2.1 Procurement lifecycle (16): Supplier → PO → Receipt → Supplier Invoice → Payment (partials supported)
- [ ] 2.2 Receiving / Stock In (17): PO alone does not increase on-hand; receipt does; cost layer created
- [ ] 2.3 Inventory Ledger UX (18): movements as source of truth; answer where/at what cost/how much remains/what consumed it
- [ ] 2.4 Multi-Warehouse / Transfers (19): location balances change, company-wide quantity preserved, no revenue/profit from transfer
- [ ] 2.5 Opening / Closing Inventory + Financial Period integration (20): opening balance create/finalize/lock; period close + carry forward
- [ ] 2.6 Reservations: on-hand vs reserved vs available modeled and enforced

## Phase 3 — Pricing + Finance (priority 4)

- [ ] 3.1 Pricing Engine completion (21): wholesale cost → retail (+default markup 20%, .99 charm) → VIP → margin/profit; offers % from retail with old_price; pricing insights/simulation; missing-price validation before publish
- [ ] 3.2 Financial Period engine (22): open / close / carry forward / closed-period protection
- [ ] 3.3 Accounting foundation (23): double-entry, chart of accounts, journal, AR/AP subledgers reconcile to GL controls; event-to-ledger architecture; debit=credit invariant
- [ ] 3.4 Kashier Reconciliation (24): gateway abstraction (mock vs real), webhook verification, idempotency, gross-vs-fee-vs-settlement preserved; belongs under Finance → Payments & Reconciliation (not top-level)
- [ ] 3.5 Financial Reporting engine (25): revenue/COGS/gross profit/gross margin derived from real transactions; filters by date/period/warehouse/product/category/customer; exportable/printable

## Phase 4 — Control Plane (priority 5)

- [ ] 4.1 RBAC 2.0 + Scopes (26): WHO + WHAT + WHERE + HOW MUCH; warehouse-scoped roles; full role editor UX; permission catalog audited, no duplicate permissions; backend authoritative
- [ ] 4.2 Approval Engine (27): large discount / refund / inventory adjustment / purchase → approval; approver authority explicit
- [ ] 4.3 Notifications 2.0 (28): event → rule → recipient → priority → action → status; recipient mapping hardened (numeric-ID bug class)
- [ ] 4.4 Audit 2.0 (29): WHO / WHAT / WHEN / WHERE / BEFORE / AFTER; business-readable; no destructive edits
- [ ] 4.5 Security: rate limits, helmet, CORS, session settings; negative security tests per sensitive mutation

## Phase 5 — AI + Deployment (priority 6–7)

- [ ] 5.1 Deployment / Publishing control plane (30–32): customer public web, admin local/internal, configurable domains, lifecycle Draft → Preview → Publish → Verify → Rollback; customer/admin environment separation
- [ ] 5.2 Admin Control Plane (31)
- [ ] 5.3 AI Operations Copilot (33–34): consumes TRUSTED data only (never before trusted data + reliable logic + observable workflows + reporting); answers reference business evidence; AI inherits user permissions/scope (never a security bypass)
- [ ] 5.4 Anomaly Detection (35): suspicious discounts, refund anomalies, stock anomalies, duplicate movements, payment mismatches
- [ ] 5.5 Executive Control Dashboard (36): Overview evolves beyond decorative KPI cards into the organization's control center

## Per-phase mandated deliverables (master prompt §29 — every new phase)

1. Audit
2. Requirement Matrix
3. Implementation
4. Unit/Integration Tests
5. Runtime Test
6. UI/API/DB Cross-check
7. Negative Security Tests
8. Retry/Idempotency tests
9. Concurrency test where relevant
10. Regression
11. Final Acceptance Matrix

## Non-negotiable invariants (master prompt §60 — never violate)

1. 5172 = Backend/API (server/)
2. 5173 = Customer (client/)
3. 5174 = Admin (client-admin/)
4. Admin source = client-admin
5. Customer source = client
6. Backend authorization is authoritative (frontend hiding is not security)
7. No fake data in production UI
8. No direct DB manipulation to fake evidence
9. One authoritative stock mutation per inventory event
10. Picking does not double-deduct inventory
11. Historical financial/order data immutable where required
12. Retry behavior intentionally idempotent (metadata protected, not just status)
13. Concurrent workflows have backend state protection
14. No duplicate business systems
15. No false VERIFIED claims

## Standing rules

- NO EMOJI in UI — custom CSS stroke icons (AdminIcon / client Icon.jsx / StoreIconPicker) only
- Arabic must be complete and single-language (no mixed Arabic/English headings); route translations through i18n dictionaries
- Keep related pages as sub-tabs of one page (site-config pattern); do not promote minor features to top-level nav domains
- Zero destructive changes; preserve historical cost/price/discount/tax/transactions; classify legacy code ACTIVE/LEGACY/UNUSED/DUPLICATE/DANGEROUS before touching
- Document updates required with every meaningful step; never claim complete unless implemented AND verified

---

## FK Insert-Path + Transactional Inventory Checkpoint — (CLOSED 2026-09-01)

Status legend: [x] VERIFIED · [!] BLOCKED/DECISION · [ ] OPEN

- [x] **FK enforcement** — `PRAGMA foreign_keys = 1`; orphan `issue_orders.order_id` rejected; duplicate `product_suppliers` UNIQUE rejected; valid parent→child accepted.
- [x] **FK insert-path compliance** — real `customers`→`orders`→`products` fixtures; `npm test` 118/118 green (all FK-exposed suites fixed).
- [x] **Bridge invalid-order guard** — `createIssueForOrder(999999)` throws clean domain error; `salesInventoryBridge.issueForOrder/reserveForOrder/releaseForOrder` also guard order existence (no dangling `reference_id`).
- [x] **Bridge transaction** — `createIssueForOrder` wraps insert+items+status; nested `db.transaction()` is SAVEPOINT-aware; nested rollback proven (0 rows persisted).
- [x] **Supply receipt transaction** — `issueSupplyOrder` wrapped in `db.transaction()`.
- [x] **Generic issue transaction** — `issueIssueOrder` wrapped in `db.transaction()` (replaced manual correction rollback).
- [x] **Payment atomicity** — `handleTransactionSuccess` release→issue→status inside one txn; latent nested-`BEGIN` bug fixed; fulfillment issue-order now created on paid webhook (was silently skipped).
- [!] **Live orphan migration** — `REQUIRES DATA-MIGRATION DECISION` (inventory_movements→products 40, issue_order_items→products 24, product_suppliers→products 2, product_suppliers→suppliers 4). Not mutated.
- [x] **Parent-deletion policy** — audited; soft-delete/deactivate preferred; no destructive cascade introduced.
- [ ] **Payment attempts** — OPEN (not in current architecture; no speculation).
- [ ] **Refunds** — OPEN / REQUIRES PROVIDER + BUSINESS DECISION.
- [ ] **Settlement/accounting** — OPEN / REQUIRES BUSINESS DECISION (no GL/journals).
- [ ] **Supplier UI** — OPEN (deferred; not started this checkpoint).
- [ ] **UI/UX closure** — OPEN (deferred; not started this checkpoint).

Evidence: `ECOM-ERP-PRODUCTION-HARDENING-CHANGELOG.md` Batch 4; `ECOM-ERP-DATA-INTEGRITY-DELETE-POLICY.md` §5.

---

## Supplier / Supply UI + UI/UX closure — (PARTIAL 2026-09-01)

Status legend: [x] VERIFIED · [~] PARTIAL · [ ] OPEN

- [x] **Supplier Detail UI** — `SupplierDetail.jsx` at `/erp/suppliers/:id`; identity/contact/lead-time, Products Supplied (Current/Latest/Highest), Purchase History, per-product cost-history cards. Admin build ✓.
- [x] **Supplier cost-history UI** — real `GET /suppliers/:id/history`; LATEST/HIGHEST/FIFO explainable; no history overwritten.
- [x] **New Supply UI** — line totals + Subtotal/Grand Total; money validation (`egpToCents`); qty>0; Issue/Cancel → ConfirmDialog.
- [x] **Suppliers list nav + ConfirmDialog** — row/View → detail; Deactivate dialog; `ms-2` RTL fix.
- [x] **Arabic** keys for supplier/supply domain.
- [x] **Backend+retail+security regression** — `npm test` 118/118; targeted 33/33; supplier e2e (layers 10@5000+10@5500); HIGHEST×25%=6875.
- [x] **Supplier↔Supply data-model link** — `supply_orders.supplier_id` FK added (nullable/additive); `createSupplyOrder` validates + stores it; supplier-history endpoint merges PO + supply receipts with a `source` field (verified e2e; no historical rows to backfill).
- [~] **Native confirm/alert migration** — Suppliers+Supply done; 15 refs across 11 files remain.
- [~] **Design system consolidation** — 77×`bg-primary-600`, 18×lucide, 8×`.btn-primary`/`.card` remain.
- [ ] **Full RTL** (91 physical props), **locale** (16 `en-GB`), **full Arabic**, **mobile-table strategy**, **focus-visible a11y** — REMAINING.
- [x] **Browser verification (admin)** — Login, Suppliers, Supply Orders (TDZ crash found+fixed), Supplier Detail all render (Chrome152 headless, CDP, 1280+390). `browser-evidence/*.png`.
- [~] **Browser verification (storefront)** — production build ✓, API 200; dev capture only Vite cold-start spinner+benign404 → **REQUIRES BROWSER VERIFICATION (production-served)**.

Evidence: `ECOM-ERP-UI-UX-CLOSURE-REPORT.md`; `ECOM-ERP-PRODUCTION-HARDENING-CHANGELOG.md` Batch 5.

---

## Order/Payment domain + sidebar IA — (2026-09-02 checkpoint)

Status legend: [x] VERIFIED · [~] PARTIAL · [!] REQUIRES DECISION/PROVIDER/DATA-MIGRATION · [ ] OPEN

- [x] **Sidebar duplicate Overview** — `Sidebar.jsx` had 3 "Overview" (pinned rail + redundant SECTIONS[0] group → nested child); removed the redundant group; now exactly one. **Browser-verified** (Chrome152/CDP): `Overview Sales Products … System` + real Orders-by-Status donut (22 orders) matches DB.
- [x] **Payment-domain audit** → `ECOM-ERP-PAYMENT-DOMAIN.md` (findings checkpoint).
- [x] **RBAC on order status route** — `PUT /api/admin/orders/:id/status` now requires `orders.update` (was adminAuth-only).
- [x] **Webhook amount/currency verification** — `handleTransactionSuccess` rejects signed-amount ≠ order.total (or currency ≠ EGP) before concluding paid; records `payment_status='verified'`, `payment_method='kashier'`, `paid_at`. `tests/paymentDomain.test.js` 3/3. `npm test` **121/121** (17 suites).
- [~] **Manual admin "paid" audit trail** (reservation + reason/actor/method) — gap documented; REQUIRES Phase-4 follow-up.
- [!] **Stale `transaction-refund` guard** (delivered/completed) — REQUIRES BUSINESS DECISION (return semantics not modeled).
- [!] **Web-checkout order-address snapshot** — `orders.shipping_*` exists + mobile checkout populates it; **web `POST /api/orders` doesn't receive/store shipping** → REQUIRES implementation on storefront checkout.
- [!] **Variant-level inventory semantics** — REQUIRES BUSINESS DECISION.
- [!] **Real Kashier sandbox + production verification** — REQUIRES PROVIDER.

---

## Employee signatures + VIP consolidation + address snapshot — (2026-09-02, Batch 8)

- [x] **Employee personal signatures** — users extended (`signature_path/mime/updated_at`); reuses multer→`public/images/signatures` storage; self (`/me`) + admin (`users.update`) routes; file-type/size validation; `users.js` `clearSignature` path bug fixed. `tests/signature.test.js` 1/1.
- [~] **Signature ↔ PDF integration** — per-user signature stored/retrievable; wiring into invoice/receipt/issue/transfer/shipping PDFs + shared branding helper = REMAINING.
- [x] **Duplicate VIP Invitations** — root cause: standalone page+sidebar+route DUPLICATED the already-existing Customers "VIP & Invitations" tab. Removed the standalone page/sidebar/route → single Customers tab. **Browser-verified** (no sidebar VIP; Customers tabs Normal/VIP/VIP Invitations).
- [!] **Dead `vipInvitations.js` (`/api/admin/vip`)** — no client consumer; left mounted (harmless), flagged for removal.
- [x] **Order-address snapshot (web checkout)** — `POST /api/orders` snapshots `shipping_*` (client wins else profile-at-order-time). `tests/addressSnapshot.test.js` 1/1 (change default → historical order unchanged).
- [x] **`ECOM-ERP-TEST-ACCOUNTS.md`** — created (actual roles: admin/sales_employee/warehouse_employee/delivery_employee; normal + VIP customers; signature + payment test purposes).
- [x] `npm test` **19 suites /123 pass**; Admin build ✓; Client build ✓.
- [ ] **Reporting/incident system, PDF redesign, manual-paid audit trail, stale-refund guard (business), variant-inventory (business), Kashier sandbox/prod (provider), full Arabic/RTL/design-system/a11y/responsive** — REMAINING.

---

## Purchase-Order fix + Kashier Payment Center — (2026-09-02, Batch 9)

- [x] **PO sequence defect** — `document_sequences` gate bug meant PO was never seeded → PO create 500'd. Fixed (idempotent per-type `WHERE NOT EXISTS`). PO page + create now work.
- [x] **PO ≠ receiving domain test** — `tests/purchaseOrder.test.js` (1/1): create/`received` do NOT change stock; only `supply_order` receiving does (+stock, +cost layer).
- [x] **Kashier Payment Center** — removed "Test Session (1 EGP)" from operational UI; professional header + honest empty state + RTL-safe table + i18n. Browser-verified (`admin-kashier.png`, `hasTestBtn:false`).
- [x] `npm test` **20 suites /124 pass**; Admin build ✓ (10.99s); Client build ✓; orphans 40/24/2/4 unchanged.
- [ ] **OPEN (major redesign, not started this cycle)**: `/erp/reports` report center · `/erp/financial-periods` honest timeline · `/erp/packing` workflow debug · PO approval/rejection/notify/RBAC receive · `/pricing-engine` rebuild cost→retail→VIP · `/erp/price-lists` consolidation (one pricing authority) · `/erp/settings` IA · reporting/incident system + notifications · PDF redesign + signature→PDF · full Arabic/RTL/design-system/a11y.

---

## Purchase Order approval/rejection workflow — (2026-09-02, Batch 10)

- [x] **PO approve/reject audit** — `purchase_orders` gained `approved_at`, `reject_reason`, `rejected_by`, `rejected_at` (idempotent); status route records approver on confirm, requires+stores rejection reason + rejector on cancel, actor from session, and notifies via existing `notificationService`. Inventory invariant preserved (approve/reject never move stock; only `/receive` does).
- [x] `tests/purchaseOrder.test.js` → **3/3** (approve actor/no-stock; reject-reason required+stored; PO≠receiving). `npm test` **20 suites /126 pass** (baseline 124 +2). Admin build ✓ (10.93s).
- [~] **Reject-reason prompt UI** in `PurchaseOrdersList.jsx` — backend authoritative + tested; the approve/reject buttons + reason dialog not yet wired to the UI.
- [ ] **OPEN**: Report Center · Financial Periods · Packing · Pricing Engine · Price Lists consolidation · Settings IA · Incidents · PDF redesign + signature→PDF · full Arabic/RTL/a11y · storefront prod verification.

---
Last updated: 2026-09-02 (PO approve/reject workflow; remaining modules OPEN)
